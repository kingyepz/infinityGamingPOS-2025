
import { createServerClient, type CookieOptions } from '@supabase/ssr'
import { cookies } from 'next/headers'

export function createClient() {
  const cookieStore = cookies()

  const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL
  const supabaseAnonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY

  // Fallback placeholders when env vars are missing (e.g., preview without secrets)
  const resolvedUrl = supabaseUrl ?? 'http://localhost'
  const resolvedKey = supabaseAnonKey ?? 'public-anon-key'

  return createServerClient(
    resolvedUrl,
    resolvedKey,
    {
      cookies: {
        get(name: string) {
          return cookieStore.get(name)?.value
        },
        set(name: string, value: string, options: CookieOptions) {
          try {
            cookieStore.set({ name, value, ...options })
          } catch (error) {
            // Intentionally ignored in Server Components
          }
        },
        remove(name: string, options: CookieOptions) {
          try {
            cookieStore.set({ name, value: '', ...options })
          } catch (error) {
            // Intentionally ignored in Server Components
          }
        },
      },
    }
  )
}
